"""Agent tools, exposed via MCP.

calendar/reminder/task CRUD, memory read/write, web_search (ddgs), ingest_document
(PDF/notes -> RAG). Defining them as an MCP server keeps them reusable by other
clients later. (phase 3-4, 7)
"""
